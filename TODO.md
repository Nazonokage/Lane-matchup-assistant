# TODO

## UI header/almanac tuning
- [x] Remove images shown on `.detail-header`
- [x] Make image inside `.rec-head` bigger
- [x] Make almanac look similar to `.rec-head` (put icon in `.card-head`)
- [x] Build succeeded (`npm run build`)


